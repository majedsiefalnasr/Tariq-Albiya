# Environment Way – RTL Bootstrap landing

- Bootstrap 5.3 RTL (CSS only, no JS)
- Arabic RTL layout with Inter + Noto Sans Arabic fonts
- All images are referenced directly from your Figma export server (localhost asset URLs)

## Run

Just open `index.html` in your browser.

## Customize

- Colors and tokens are in `styles.css` under `:root` (brand, background, radii, etc.)
- Max content width is 1116px to match the Figma.
